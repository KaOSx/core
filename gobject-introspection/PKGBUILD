
pkgname=gobject-introspection
pkgver=1.76.1
_pkgver=1.76
pkgrel=1
pkgdesc="Introspection system for GObject-based libraries"
url="https://wiki.gnome.org/Projects/GObjectIntrospection"
arch=('x86_64')
license=('LGPL' 'GPL')
depends=('libffi' 'glib2' 'python3')
makedepends=('cairo' 'meson' 'ninja')
options=('!libtool')
source=("https://ftp.gnome.org/pub/gnome/sources/${pkgname}/${_pkgver}/${pkgname}-${pkgver}.tar.xz")
sha256sums=('196178bf64345501dcdc4d8469b36aa6fe80489354efe71cb7cb8ab82a3738bf')

build() {
  mkdir -p build
  cd build

  meson setup ../${pkgname}-${pkgver} \
    --prefix=/usr \
    --buildtype=release \
    -D python=python3
  ninja
}

package() {
  cd build

  DESTDIR=${pkgdir} ninja install
}

