
pkgname=mpfr
_pkgver=4.2.0
_patchlevel=p7
pkgver=${_pkgver}.${_patchlevel}
#pkgver=${_pkgver}
pkgrel=1
pkgdesc="Multiple-precision floating-point library"
arch=('x86_64')
url="https://www.mpfr.org/"
license=('LGPL')
depends=('gmp')
options=('!libtool')
source=("https://www.mpfr.org/mpfr-current/mpfr-${_pkgver}.tar.xz"
        # https://www.mpfr.org/mpfr-4.2.0/allpatches  http://www.mpfr.org/mpfr-4.2.0/#bugs
        'allpatches.diff')
        #"mpfr-${_pkgver}-${_patchlevel}.patch")
sha256sums=('06a378df13501248c1b2db5aa977a2c8126ae849a9d9b7be2546fb4a9c26d993'
            'c4570f9ed20cee8280c59fedddce6e16ee5b8ededa7004af13c6cacbc487bb93')

build() {
  cd ${pkgname}-${_pkgver}
  patch -p1 -i $srcdir/allpatches.diff

  ./configure --prefix=/usr --enable-thread-safe --enable-shared
  make
}

check() {
  cd ${pkgname}-${_pkgver}

  make check
  make check-exported-symbols
}

package() {
  cd ${pkgname}-${_pkgver}

  make DESTDIR=${pkgdir} install
}
