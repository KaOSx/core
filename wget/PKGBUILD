
pkgname=wget
pkgver=1.19.1
pkgrel=1
pkgdesc="A network utility to retrieve files from the Web"
arch=('x86_64')
url="https://www.gnu.org/software/wget/wget.html"
license=('GPL3')
groups=('base')
depends=('glibc' 'openssl' 'libidn' 'util-linux' 'pcre' 'libpsl')
optdepends=('ca-certificates: HTTPS downloads')
backup=('etc/wgetrc')
source=("https://ftp.gnu.org/gnu/wget/${pkgname}-${pkgver}.tar.xz"
        '0001-main.c-Need-to-explicitly-disallow-show_progress-in-.patch')
md5sums=('d30d82186b93fcabb4116ff513bfa9bd'
         '87ebe95f2c403e041fab209610e695ae')

build() {
  cd ${pkgname}-${pkgver}
  patch -p1 -i $srcdir/0001-main.c-Need-to-explicitly-disallow-show_progress-in-.patch
  
  cat >> doc/sample.wgetrc <<EOF

# default root certs location
ca_certificate=/etc/ssl/certs/ca-certificates.crt
EOF

  ./configure --prefix=/usr --sysconfdir=/etc -with-ssl=openssl --enable-nls
  make
}

package() {
  cd ${pkgname}-${pkgver}
  
  make DESTDIR=${pkgdir} install
}
